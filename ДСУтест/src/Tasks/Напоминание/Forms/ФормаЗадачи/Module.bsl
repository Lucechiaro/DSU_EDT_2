&НаКлиенте
Процедура ПередВыполнением(Отказ)
	
	// откроем форму документа "Расход денежных средств"
	ФормаРасхода = ПолучитьФорму("Документ.РасходДенежныхСредств.ФормаОбъекта", Новый Структура("Основание", Объект));
	ФормаРасхода.ОткрытьМодально();
	Если Не ФормаРасхода.Объект.Проведен Тогда
		Отказ = Истина;
	КонецЕсли;	
	
КонецПроцедуры

